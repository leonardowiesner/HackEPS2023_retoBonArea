export class PlanogramTable {
        constructor({ x, y, picking_x, picking_y, description}) {
            this.x = x;
            this.y = y;
            this.picking_x = picking_x;
            this.picking_y = picking_y;
            this.description = description;
        }
    }
  