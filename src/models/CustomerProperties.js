export default class CustomersProperties {
  constructor({ id, step_seconds, picking_offset }) {
    this.id = id;
    this.step_seconds = step_seconds;
    this.picking_offset = picking_offset;
  }
}
