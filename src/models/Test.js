export default class Test {
  constructor({ customer_id, ticket_id, x, y, picking, x_y_date_time }) {
    this.customer_id = customer_id;
    this.ticket_id = ticket_id;
    this.x = x;
    this.y = y;
    this.picking = picking;
    this.x_y_date_time = x_y_date_time;
  }
}
