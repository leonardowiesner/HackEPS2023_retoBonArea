export class CustomersProperties {
    constructor({customer_id, step_seconds, picking_offset}) {
      this.customer_id = customer_id;
      this.step_seconds = step_seconds;
      this.picking_offset = picking_offset;
    }
  }
